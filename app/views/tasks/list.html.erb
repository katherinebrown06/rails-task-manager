<h1>Pick a task from the list below!</h1>

<p>
<ul>
<% @tasks.each do |task|%>
  <li><%= link_to task.title, task_path(task) %> </li>
<% end %>
</ul>

<%= link_to task.new, tasks_new_path(task) %>
</p>
